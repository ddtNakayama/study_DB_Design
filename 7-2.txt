
■マテリアライズドビュー

	・実体を持つ。実際のテーブルから取得した時点の情報を持つ（スナップショット）。
	・実際のテーブルが更新された際には、マテリアライズドビューに更新が反映される（リフレッシュ）。
	・複雑な検索処理を高速で取得できる。
	
■ビュー

	・ビューは実態を持たない。実際のテーブルと紐づいている。
	・集計結果などをビューのカラムとして持つことで、簡単に値を得ることができる。ただし、都度クエリが実行される。